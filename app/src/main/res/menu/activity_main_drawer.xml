<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_inbox"
            android:icon="@drawable/ic_inbox"
            android:title="@string/drawer_inbox" />
        <item
            android:id="@+id/nav_calendar"
            android:icon="@drawable/ic_calendar"
            android:title="@string/drawer_calendar" />
    </group>

    <item android:title="@string/drawer_projects">
        <menu>
            <group
                android:id="@+id/group_projects"
                android:checkableBehavior="single">
            </group>
        </menu>
    </item>

    <item
        android:id="@+id/nav_add_list"
        android:icon="@drawable/ic_add"
        android:title="@string/drawer_add_list"
        android:orderInCategory="100" />

</menu>
